[pytest]
# Garante que o módulo conversor_core seja importável tanto por `pytest`
# quanto por `python -m pytest`, independentemente do executável usado.
pythonpath = .
testpaths = tests
# Evita criar `.pytest_cache` em ambientes educacionais ou sandboxes restritos.
addopts = -p no:cacheprovider
